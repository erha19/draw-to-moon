# 一笔滚月 v2 验证记录

验证日期：2026-09-17。

## 已完成

- `npm test`：11 项通过。圆度、方向和采样密度；缺口/直线/八字/小圈；单圈自动滚动；圆度与距离单调性；30/60/120 FPS 一致；终止后不再变化；新版本结果和轮廓保留；3D 网格确定性与封闭性；朝外三角面和单位法线；不规则轮廓保留；OBJ 顶点索引。
- `npm run check`：应用、引擎、3D 渲染器、海报、服务器语法检查通过。
- Playwright + 本机 Chrome：390×844 无横向溢出；无倒计时；小圈错误可重画；仅画一次后自动生成模型、滚动、结算；无需后续点击；跳过动画仍保留最终距离；刷新后恢复收藏月亮；OBJ 下载；1080×1440 PNG 生成和下载；PNG 导出失败重试；取消系统分享保留图片；距离挑战链接；运行时错误为零。
- 人工截图检查：绘画页、自动滚动、旋转月亮结算、海报、首页。立体月亮使用真实轮廓，青金配色一致，距离与圆度可读。
- 3D 模块局部基准：桌面 Chrome 400×400、DPR 2，约 1.49ms/帧；仅代表该测试环境，不代表手机性能。

## 未完成的外部验收

- iPhone Safari、安卓和微信内触摸、长按保存及系统分享目标。
- 真实用户手感及距离参数平衡。
- 公网 HTTPS 部署后的跨设备挑战链接。
- 全国用户成绩采集、校验及真实百分位。当前未展示全国胜率。

OBJ 保存几何与法线；月坑外观为程序纹理，未烘焙进 OBJ。滚动采用确定性游戏模型，不宣称精确刚体物理。

## 2.1 赠月海报与单屏布局

- 8 个视口 × 首页/绘画/滚动/结算/海报 5 状态均通过：无文档纵横溢出，主要控件及 Canvas 均在视口内。
- 检查手机小屏、390px 常见视口和1440px桌面截图；首页采用一体场景，结果模型和祝福海报完整可见。
- 导出 1080×1440 PNG，文案常量精确匹配「送你一颗中秋的月亮」「愿你所念皆圆满，所行皆坦途。」。
- 游戏完整浏览器回归通过，包括单次绘画、自动结算、OBJ/祝福PNG下载、收藏恢复、海报失败重试和分享取消。
- 月亮独立按 900×900 输出，用于高清海报；不依赖结果页可用高度。
- 当前仍没有真实手机保存至相册的验收证据。

## 2.2 玉兔蓄力推月过场

验证日期：2026-09-17。

- `npm test`：22 项通过，包含原有 11 项计分/模型测试、3 项过场时序与减少动态效果测试、8 项音频能力与节点生命周期测试。
- `npm run check`：全部应用脚本语法检查通过。无新增依赖。
- `tests/rabbit-push.mjs`：11 项专项浏览器场景通过，验证六个姿态、2.8 秒前距离保持零、暂停恢复、跳过结算为 2000 米、退出重玩不残留、动态切换减少动态效果、静音持久化与即时中止、无音频能力或图集失败时继续游戏。
- 专项检查使用真实 Web Audio 节点记录验证静音、暂停、跳过和退出的声音取消；离线渲染确认合成序列有非零信号且未削波。真实设备的扬声器听感和 Safari/iOS 音频恢复仍需验收。
- 原有完整浏览器回归通过：自动结算、收藏、OBJ/PNG 下载、海报重试、分享取消与挑战链接。
- 原有 8 个视口 × 5 个状态布局验收通过；新增 390×844 推月布局，以及 320×568、844×390 下全部六个姿态的实际非透明像素边界检查。
- 最终截图检查覆盖桌面、普通手机、小屏、横屏和挥手姿态。玉兔双爪与实际 3D 月亮接触、脚底对齐地面、图集透明、姿态完整；横屏字幕未被圆角裁切。
- 玩家月亮仍由原始轮廓建模，过场只影响表现，未修改 `engine.js` 的距离或圆度规则。
